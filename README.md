# Easy Job

BOSS 直聘网页端浏览器扩展，提供岗位获取、规则过滤、AI 匹配、招呼语生成、投递控制与运行记录管理。

## 特性

- 双来源取岗：搜索条件与 BOSS 求职期望，写入同一投递池，支持中断恢复。
- 内置去重与硬性过滤：已沟通、同公司、同招聘者、猎头岗位、公司规模、工作内容关键词、招聘者活跃度。
- JD 匹配与招呼语生成基于用户自建的 AI 服务，支持 Chat Completions、Responses 与 Anthropic Messages 协议。
- 简历、模型配置与插件配置的本地导入导出。
- 投递记录与运行日志持久化，用于定位自动化流程异常。

## 安装

支持 Chrome、Edge 与 Firefox，[Releases](../../releases) 按浏览器分别提供构建产物，均为 Manifest V3。

| 浏览器  | 产物                          | 载入方式                                                                                   |
| ------- | ----------------------------- | ------------------------------------------------------------------------------------------ |
| Chrome  | `easy-job-<版本>-chrome.zip`  | 解压后，`chrome://extensions` 开启开发者模式 →「加载已解压的扩展程序」选中该目录           |
| Edge    | `easy-job-<版本>-edge.zip`    | 解压后，`edge://extensions` 开启开发人员模式 →「加载解压缩的扩展」选中该目录               |
| Firefox | `easy-job-<版本>-firefox.zip` | `about:debugging#/runtime/this-firefox` →「临时载入附加组件」选中 zip 内的 `manifest.json` |

Chrome 与 Edge 解压出的目录不能删除，浏览器每次启动都从该目录加载。

Firefox 要求 115 ESR 或更高版本。临时载入的扩展在浏览器重启后失效，永久安装需要经 Mozilla 签名。

在已登录的 BOSS 直聘页面点击扩展图标唤起面板。

## 面板

|     | 分区     | 内容                                             |
| --- | -------- | ------------------------------------------------ |
| 01  | 投递控制 | 启停投递；今日进度、来源效率、失败归因、任务序列 |
| 02  | 投递记录 | 已投岗位，含来源、匹配度与结果状态               |
| 03  | 岗位规则 | 搜索来源与求职期望来源配置                       |
| 04  | 运行配置 | 队列节奏、岗位过滤、岗位详情、AI 与招呼语        |
| 05  | 个人信息 | 简历、AI 模型、配置导入导出                      |
| 06  | 运行日志 | 运行过程日志                                     |

任务序列为：获取岗位 → JD 筛选 → 生成招呼语 → 建立沟通。

## 配置

### AI 服务

在「个人信息」配置 API 地址、Key、模型名与协议类型，保存前可执行连接测试。请求由浏览器直接发往所配置的地址，项目不提供任何中转服务。

简历以正文形式录入，由模型解析为结构化证据；匹配打分与招呼语均以该证据为输入，而非简历原文。未配置模型时简历可以保存，解析推迟到模型可用后执行。

AI 匹配与 AI 招呼语默认关闭，且不会因模型配置完成而自动启用。

### 岗位来源

搜索来源与求职期望来源可独立启用。

搜索来源接受多个搜索词，各方向分别取岗，另可配置城市、薪资、工作经验、学历与求职类型。**未填写任何搜索词时该来源不取岗**：空搜索词等价于全站检索，不作为有效配置处理。

求职期望来源读取账号在 BOSS 侧已设置的求职期望列表，逐项启用。

### 运行规则

| 分组        | 可配项                                                           |
| ----------- | ---------------------------------------------------------------- |
| 队列与节奏  | 来源取岗比例、单 JD 耗时、招呼语分段间隔、批量休息               |
| 岗位过滤    | 已沟通/好友状态、同公司去重、同 HR 去重、猎头岗位                |
| 岗位详情    | 工作内容关键词排除、招聘者活跃度                                 |
| AI 与招呼语 | AI 匹配开关与最低匹配度、AI 招呼语开关、单次消息条数、目标总字数 |

公司规模过滤为内置项，使用预设值，未开放配置。通勤距离在面板中标记为「该功能暂未开放」，开关不可用，过滤器不参与执行。

最低匹配度决定投递的准入线。评分先判定职能一致性再评估能力覆盖：职能不符上限 35；岗位描述无法提炼出可核对要求的上限 55。

## 两层过滤

**获取层**（岗位规则）决定哪些岗位被取回。城市、薪资、经验、学历等条件拼入 BOSS 搜索链接，因此仅对搜索来源生效；求职期望来源的等价条件位于 BOSS 侧，不由本扩展控制。

**过滤层**（运行配置）决定取回的岗位中哪些被投递，不区分来源，两个来源共用同一条流水线。

## 开发

要求 Node.js ≥ 20.19 与 Corepack。

```bash
corepack pnpm install
corepack pnpm dev
```

构建与校验：

```bash
corepack pnpm test
corepack pnpm check
corepack pnpm lint
corepack pnpm fmt:check
corepack pnpm verify:public-config
corepack pnpm build     # 三个目标全量构建并逐个校验 manifest
corepack pnpm zip       # 产出三个浏览器的分发包
```

单目标可用 `build:chrome` / `build:firefox` / `build:edge`，各自附带校验。产物输出至 `.output/`，不纳入版本控制。

## 数据与网络

插件配置、简历、运行日志与投递记录存储于浏览器扩展本地存储。启用 AI 功能后，扩展会将任务所需的简历证据与岗位信息发送至用户自行配置的模型服务。

详见 [PRIVACY.md](./PRIVACY.md) 与 [SECURITY.md](./SECURITY.md)。

## 免责声明

**本项目仅供学习交流，禁止用于商业用途。**

**使用该脚本有一定风险（如黑号、封号、权重降低等），本项目不承担任何责任。**

以上为作者立场声明。本项目以 MIT License 开源，MIT 条款本身不限制商业使用，该声明因此不构成授权限制。选择无视者自行承担由此产生的全部后果，作者不提供任何形式的担保，亦不承担任何责任。

使用自动化投递前，请自行确认招聘平台规则、账号风险、投递节奏及所在地适用的法律法规。因使用本项目导致的账号封禁、数据丢失、法律纠纷或其他损失，均由使用者自行承担。

请勿将真实 API Key、简历、Cookie、运行日志或导出的个人配置提交到公开仓库。

## License

[MIT](./LICENSE)
